import Action from '../../app/scripts/components/actions/apiActions';
import dispatcher from '../../app/scripts/components/dispatcher';

describe('searchThroughSources function', () => {
  function handleAction() {

  }
  dispatcher.register(handleAction);
  it('sends the value of the input element', () => {

  });
});
